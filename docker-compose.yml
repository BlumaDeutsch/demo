version: '3.8'

services:
  backend-service:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - DB_URL=${DB_URL}
      - JWT_SECRET=${JWT_SECRET}

  frontend-service:
    build: ./client
    ports:
      - "3000:3000"